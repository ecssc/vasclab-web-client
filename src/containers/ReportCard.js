import { connect } from 'react-redux';

import BaseReportCard from '../components/ReportCard';

const mapStateToProps = (state) => ({
    //
});

const mapDispatchToProps = (dispatch) => ({
    //
});

const ReportCard = connect(mapStateToProps, mapDispatchToProps)(BaseReportCard);

export default ReportCard;
